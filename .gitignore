# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.idea
.vscode
*.swp
*.swo
*~

# mypy / pytest / ruff
.mypy_cache/
.pytest_cache/
.ruff_cache/

# uv lockfile (library project — let consumers resolve)
uv.lock

# Per-session Claude / agent artifacts (host-local; not project content)
.claude/skills
.claude/scripts/
.claude/agents
.claude/commands
.claude/settings.local.json
.mcp.json
MEMORY.md
.cc-voice.toml

# Host shell / tool dotfiles surfaced by sandbox bind-mounts (not project content)
.bash_profile
.bashrc
.env
.gitconfig
.profile
.ripgreprc
.zprofile
.zshrc

# Host-local runtime config (rendered per-host; e.g., config.local/pipettebot.env)
config.local/

# Live serial captures (keep dir, ignore data)
captures/*.log
captures/*.bin
captures/*.txt
!captures/.gitkeep

# 3D-parts pipeline (generated artifacts, soft-cloned tooling)
hardware/stl/
hardware/svg/
hardware/gcode/
diagramforge/

# 3D scan: track raw input (pipette_scan_raw.stl) as canonical source;
# derived outputs are re-runnable via tools/cad/scan/clean_pipette_scan.py
hardware/3d-scan/dpette_scan.stl

# Untracked example output
showcase_v0_pipette_sim.gcode
showcase_v0_full_plate.gcode
showcase_v0_full_pipettebot.gcode
showcase_v0_tip_pickup_release.gcode
showcase_v0_full_pipettebot_rows.gcode

# OS
.DS_Store
Thumbs.db
